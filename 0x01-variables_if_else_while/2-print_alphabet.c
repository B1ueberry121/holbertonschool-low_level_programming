#include <stdio.h>

/**
 *main-generates a random number, then prints a string
 *Return: Ends the program
 */

int main(void)
{
	char ch = 97;

	while (ch != 123)
	{
		while (ch != 113)
		{
			putchar(ch);
		}
	}
	putchar('\n');
	return (0);
}
